# Vector concept assets

These SVGs are lightweight source/concept artwork committed with the starter so the repository stays self-contained.

Runtime animation is procedural; the apps do not require these SVGs to function.

For production, replace them with high-resolution layered artwork (PNG/WebP/HEIF + transparent foreground layers or GLB models).